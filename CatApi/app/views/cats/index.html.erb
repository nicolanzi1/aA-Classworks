<h1>All The Cats!</h1>

<ul>
    <% @cats.each do  |cat| %>
        <li>
            <%= cat_link(cat) %>

            <%= button_to "Destroy Cat!", cat_url(cat), method: :delete %>
        </li>
    <% end %>
</ul>

<%= link_to "New Cat!", new_cat_url %>